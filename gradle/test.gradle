task calc_module doLast {
    Project proj = project
    Configuration configuration = proj.configurations.findByName("chargeDebugCompileClasspath")
    println("find it $configuration")

    try {
        println(configuration.resolvedConfiguration)
    } catch (Exception e) {
        e.printStackTrace()
    }



//    configuration.resolvedConfiguration.lenientConfiguration.allModuleDependencies.forEach { module ->
//        println("module == ${module.name}")
//        String moduleString = "$module.moduleGroup $module.moduleName:$module.moduleVersion "
//        println("module == " + moduleString)
//        if (module.name.contains("dateview")) {
//            println("ccccccc")
//        }
//
//    }
//    configuration.resolvedConfiguration.resolvedArtifacts.forEach { resolvedArtifact ->
//        println("resolve==" + resolvedArtifact.name)
//    }

}