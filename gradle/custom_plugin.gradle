apply plugin: 'phoenix-plugin'

phoenix {

    transform {
        catOn = false
        tigerOn = false
        trackOn = true
        tigerClassList = ["com/bumptech/glide/RequestManager": ["load"]]
    }
}